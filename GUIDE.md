# AudioSlicer V-0.3 使用指南（零基础版）

## 📱 什么是 AudioSlicer？

**AudioSlicer** 是一个帮助你自动将长音频文件分割成短片段的工具。

**应用场景：**
- 📚 将长播客或讲座分成多个章节
- 🎵 将长录音分成多个歌曲或片段
- 📖 将有声书按自然停顿分章节

**工作原理：** 程序会自动找出音频中的"静音"部分（比如说话人停顿的地方），然后在这些地方切割，生成多个小文件。

---

## 🚀 快速开始（5分钟）

### 第1步：检查系统要求

打开"终端"应用（Mac 内置），粘贴以下命令并按 Enter：

```bash
python3 --version
```

你应该看到 `Python 3.8` 或更新的版本。如果看到 `command not found`，说明 Python 未安装。

### 第2步：安装必要软件（第一次使用）

#### 选项 A：使用 Homebrew（推荐）

1. 打开"终端"
2. 粘贴以下命令（会要求输入密码）：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

3. 当安装完成后，运行：

```bash
eval "$(/opt/homebrew/bin/brew shellenv)"
brew install ffmpeg
brew install python@3.11
brew install python-tk@3.11
```

4. 安装 Python 依赖：

```bash
pip3.11 install pydub
```

#### 选项 B：手动安装（如已有 Homebrew）

```bash
brew install ffmpeg
brew install python@3.11
brew install python-tk@3.11
pip3.11 install pydub
```

### 第3步：验证安装

在终端运行：

```bash
python3.11 - <<'EOF'
from pydub import utils
ffmpeg = utils.which('ffmpeg')
if ffmpeg:
    print(f"✓ 安装成功！ffmpeg 位置: {ffmpeg}")
else:
    print("✗ ffmpeg 未找到，请检查 Homebrew 安装")
EOF
```

---

## 📖 使用 AudioSlicer GUI（图形界面）

### 启动应用

打开终端，进入 AudioSlicer 文件夹，运行：

```bash
cd "/Users/你的用户名/AI TEST/Audio Test"
python3.11 AudioSlicer.py
```

一个窗口将弹出。

### 使用步骤

1. **选择音频文件**
   - 点击 "Choose Audio File" 按钮
   - 选择一个 `.mp3` 或 `.m4a` 文件
   - 选中的文件路径会显示在界面上

2. **选择输出文件夹**
   - 点击 "Choose Output Dir" 按钮
   - 选择一个文件夹（分割后的片段会保存在这里）
   - 最好使用一个空文件夹，避免覆盖其他文件

3. **调整静音时长阈值**
   - 使用滑块（300–1000 ms）
   - **默认值：500 ms**（适合大多数情况）
   - 更小的值（如 300 ms）= 更容易分割（即使是短停顿）
   - 更大的值（如 1000 ms）= 只在长停顿处分割

   **小提示：** 
   - 播客 / 讲座：使用 500–800 ms
   - 音乐或连贯讲话：使用 800–1000 ms
   - 有多个短句子：使用 300–500 ms

4. **开始分割**
   - 点击 "Start Slicing" 按钮
   - 程序会显示进度条和当前状态（例如：Exporting 001.mp3 (1/10)）
   - 等待完成（根据文件大小需要几秒到几分钟）
   - 完成后会看到"Completed"和一个提示窗口

5. **查看结果**
   - 打开你之前选择的输出文件夹
   - 你会看到：`001.mp3`, `002.mp3`, `003.mp3` ...
   - 每个文件对应音频的一个段落

---

## 🛠️ 无 GUI 测试模式（命令行）

如果 GUI 无法启动（例如 macOS 版本太旧），可以用测试模式：

### 运行测试（验证一切正常）

```bash
python3 test_complete.py
```

你会看到详细的测试结果。

### 运行演示切割

```bash
python3 headless_test.py
```

这会在 `test_output/` 文件夹生成示例分割结果。

---

## 📋 音频格式支持

**支持输入：** `.mp3`, `.m4a`（其他格式可能需要额外配置）

**输出格式：** `.mp3`（自动生成）

**命名规则：** `001.mp3`, `002.mp3`, `003.mp3` ...（零填充，方便排序）

---

## ❓ 常见问题

### Q1：如何选择合适的"静音时长"？

**A：** 试试这个方法：
1. 用较小值（500 ms）先试一次
2. 查看生成的片段
3. 如果分割点不对：
   - 分割过多 → 增加到 800 ms
   - 分割过少 → 减少到 300 ms
4. 再试一次

### Q2：为什么没有声音被检测到？

**A：** 可能的原因：
- **音频太连贯** → 没有静音部分（如背景音乐）
- **静音太短** → 时长设置太高，尝试降到 300 ms
- **音量设置** → 某些录音方式导致无法检测

**解决方案：** 试试不同的时长设置，或检查音频本身是否有停顿。

### Q3：导出时说"ffmpeg not found"是什么意思？

**A：** ffmpeg 没有正确安装。

**解决方案：**
```bash
brew install ffmpeg
```

然后重试。

### Q4：可以输出其他格式（如 WAV）吗？

**A：** 当前版本只支持 MP3 输出。如需其他格式，可以：
- 用音频编辑软件（如 GarageBand）批量转换
- 或告诉我，我可以添加此功能

### Q5：能处理超大文件吗？

**A：** 可以，但会占用内存。对于 > 1 GB 的文件，建议：
1. 先用其他工具分割成小块
2. 再用 AudioSlicer 处理各块

### Q6：如何删除生成的文件？

**A：** 直接删除输出文件夹中的 `.mp3` 文件即可。

---

## 🔧 高级用法

### 修改静音检测阈值（-50 dB）

默认使用 **-50 dB** 作为静音阈值。如需修改，编辑 `AudioSlicer.py`：

找到这一行：
```python
thresh_db = -50  # fixed threshold per spec
```

改成你想要的值，例如 `-45` 或 `-55`。保存后重启应用。

**说明：**
- 更大的负数（如 -55）= 更敏感（容易误判背景噪音）
- 更小的负数（如 -40）= 更宽松（可能漏掉轻微静音）

### 批量处理多个文件

当前版本只支持一次处理一个文件。如需批量处理，可以：
1. 重复运行 AudioSlicer（每次一个文件）
2. 或告诉开发者，可能会添加此功能

---

## 🐛 故障排除

| 问题 | 症状 | 解决方案 |
|------|------|--------|
| ffmpeg 不可用 | "ffmpeg not found" | `brew install ffmpeg` |
| pydub 未安装 | "No module named 'pydub'" | `pip3 install pydub` |
| GUI 无法启动 | macOS 版本错误 | 使用 `headless_test.py` |
| 文件导出失败 | 进度条卡住 | 检查输出文件夹是否有写入权限 |
| Python 找不到 | "command not found: python3" | 需要安装 Python 3.8+ |

---

## 📞 获取帮助

1. **查看日志信息** — 根据错误信息 Google 搜索
2. **运行完整测试** — `python3 test_complete.py` 帮助诊断
3. **重新安装依赖** — `pip3 install --upgrade pydub`

---

## 📝 技术细节（可选阅读）

### 工作流程

1. **加载音频** → 从文件读取音频数据
2. **检测静音** → 找出音量低于 -50 dB 且持续 >= 设定时长的区间
3. **分割** → 在静音处切割音频
4. **导出** → 保存各片段为编号 MP3 文件

### 默认参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 静音阈值 | -50 dB | 判断"静音"的音量标准 |
| 最小静音时长 | 300–1000 ms | 用户可调整 |
| 输出格式 | MP3 | 通用兼容性 |
| 编码比特率 | 128 kbps（默认） | pydub 自动设置 |

---

## ✨ 最后提示

- **第一次使用** → 用一个小文件（< 5 MB）测试
- **调整参数** → 根据结果多试几次
- **备份原文件** → 分割后原文件不会被删除，但保险起见还是备份
- **耐心等待** → 大文件处理需要时间，不要强制关闭

---

**祝你使用愉快！** 🎉

如有问题，请参考上面的"常见问题"或"故障排除"章节。
